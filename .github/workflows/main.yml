name: React build
on: 
  push:
    branches:
      - master
    paths:
      - 'front/**'
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout master
        uses: actions/checkout@master

      - name: Install dependencies
        run: npm install

      - name: Build 
        run: npm build
